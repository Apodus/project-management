dist
node_modules
pnpm-lock.yaml
.turbo

# Historical prose artifacts — design docs and generated roadmaps are
# hand-authored markdown (aligned tables, embedded snippets) that prettier's
# markdown formatter would reflow into noise. Not enforced as code style.
docs
roadmaps

# Generated artifact — owned by `openapi:export` (exact JSON.stringify(spec,
# null, 2), no trailing newline). The drift-guard test compares it byte-for-byte
# to the generated spec, so prettier must NOT reformat it.
packages/server/openapi.json
